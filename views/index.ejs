<!DOCTYPE html>
<html>

<%- include includes/head %>

<body>
    <%- include includes/search-bar %>
    <div class="container">
        <div id="movies">
            <% movies.forEach(function(movie){ %>
            <%- include("includes/master", { movie: movie, isEditOn: false }); %>
            <% }); %>
        </div>
    </div>
    <% include includes/scripts %>
</body>

</html>
