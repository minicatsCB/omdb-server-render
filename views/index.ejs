<!DOCTYPE html>
<html>

<%- include includes/head %>

<body>
    <div class="container">
        <%- include includes/search-bar %>
        <div id="movies">
            <% movies.forEach(function(movie){ %>
            <%- include("includes/movie", {movie: movie}); %>
            <% }); %>
        </div>
    </div>
    <% include includes/scripts %>
</body>

</html>
